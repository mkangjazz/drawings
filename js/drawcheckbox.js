Canvas.prototype.drawCheckbox = function(){
//      function drawCheckbox(x, y, size) {
//        drawRoundedRectangle(x, y, size, size, size/10);
//        drawCheckmark(x + size/3.25, y + size/2, size/2);
//      }
};
